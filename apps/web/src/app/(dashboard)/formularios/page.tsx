import { FormulariosClient } from '@/components/formularios/FormulariosClient'

export const metadata = { title: 'Formularios de Inicio — SPG Jurídico' }

export default function FormulariosPage() {
  return <FormulariosClient />
}
